<%= form_for @subscription do |f| %>
    <%= f.hidden_field :community_id, value: community_id %>
    <div class="message">
        <%= f.submit "Join Community", class: "submit" %>
    </div>
<% end %>